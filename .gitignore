# Python bytecode caches (any level)
__pycache__/
*.py[cod]

# Jupyter checkpoints (any level)
**/.ipynb_checkpoints/

# Repo-root generated outputs
# Note: /halo_selection is ignored so no ad-hoc cache files get committed, but
# the two tracked selection lists (*_halo_sel_1pmbin.txt, the 452-halo FoF
# sample loaded via src/paths.halo_selection_file) are intentionally tracked.
/images
/gas_fractions
/halo_selection
/results
/gp_train_results
/mcmc_chains
/cross-match
/Notebooks/validation/halo_plots
/scripts/cross-validation/best_models_k10_nbins10
/scripts/cross-validation/best_models_k10_nbins15
